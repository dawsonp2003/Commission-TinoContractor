services:
  - type: web
    name: tino-contractor-demo
    runtime: node
    rootDir: site
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: WEB3FORMS_ACCESS_KEY
        sync: false
